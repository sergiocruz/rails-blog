<!DOCTYPE html>
<html>
<head>
  <title><% if @pagetitle.present? %><%= @pagetitle %> | <% end %>Rails Blog</title>
  <meta id="viewport" name="viewport" content="width=device-width; initial-scale=1; maximum-scale=1; user-scalable=0;" />
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "Rails Blog", posts_path, :class => "brand" %>
      <div class="nav-collapse collapse">
        <p class="navbar-text pull-right">
          <% if @current_author.present? %>
            Hi <%= @current_author.name %>!
          <% end %>
        </p>
        <ul class="nav">
          <li><%= link_to "Home", root_url %></li>
          <li><%= link_to "Blog", posts_path %></li>
          <% if @current_author.present? %><li><%= link_to "New post", :controller => "posts", :action => "new" %></li><%end%>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container-fluid">

  <% if flash[:notice] %>
    <!-- Notice -->
    <div class="alert alert-info">
      <%= flash[:notice] %>
    </div>
  <% end %>

  <%= yield %>

</div><!-- .container-fluid -->
</body>
</html>
